# Environment variables for Browsergame
# Copy this file to .env and adjust the values as needed

# Database Configuration
DB_HOST=localhost
DB_NAME=browsergame
DB_USER=browsergame
DB_PASSWORD=sicheresPasswort

# For Docker setup, these will be automatically set by docker-compose.yml
# For manual installations, adjust these values according to your database setup